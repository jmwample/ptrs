
/*
const SEED_LENGTH: usize = 24;

enum PacketType {
    Payload(Vec<u8>),

    // control channel messages
    PrngSeed([u8; SEED_LENGTH]),
    Parameters(Vec<u8>),
    HeartBeat(Vec<u8>),

    // crypto messages
    OfferKyber(OfferKyberAKE),
    AcceptKyber(AcceptKyberAKE),
}

struct OfferKyberAKE {}
struct AcceptKyberAKE {}
*/
